---
import config from "@/config/config.json";
import Base from "@/layouts/Base.astro";
import PageHeader from "@/partials/PageHeader.astro";
import { getEntry } from "astro:content";

const contact = await getEntry("contact", "-index-cy");
const { contact_form_action }: { contact_form_action: string } = config.params;
const { title, description, meta_title, image } = contact.data;
---

<Base
  title={title}
  meta_title={meta_title}
  description={description}
  image={image}
>
  <PageHeader title={title} />
  <section class="section-sm">
    <div class="container">
      <div class="row">
        <div class="mx-auto md:col-10 lg:col-6">
          <form action={contact_form_action} method="POST">
            <div class="mb-6">
              <input type="hidden" name="access_key" value="849b9409-b65b-4524-8ccc-084d05bbb148">
              <input type="hidden" name="subject" value="Cyflwyno Ffurflen Gyswllt Newydd">
              <input type="hidden" name="from_name" value="Gwefan Gwaith Saer a Chrefftau i Bawb">
              <input type="checkbox" name="botcheck" class="hidden" style="display: none;">
              <label for="name" class="form-label">
                Enw Llawn <span class="text-red-500">*</span>
              </label>
              <input
                id="name"
                name="name"
                class="form-input"
                placeholder="Siôn Dafydd"
                type="text"
              />
            </div>
            <div class="mb-6">
              <label for="email" class="form-label">
                Ebost <span class="text-red-500">*</span>
              </label>
              <input
                id="email"
                name="email"
                class="form-input"
                placeholder="sion.dafydd@ebost.com"
                type="email"
              />
            </div>
            <div class="mb-6">
              <label for="message" class="form-label">
                Neges <span class="text-red-500">*</span>
              </label>
              <textarea
                id="message"
                name="message"
                class="form-input"
                placeholder="Rhowch eich neges yma..."
                rows="8"
              ></textarea>
            </div>
            <div id="hcaptcha-container" class="h-captcha mb-6" 
                  data-captcha="true" 
                  data-lang="cy" 
                  data-theme="light"
            ></div>
            <button type="submit" class="btn btn-primary">Anfon</button>
          </form>
        </div>
      </div>
    </div>
  </section>
</Base>
<script src="https://web3forms.com/client/script.js" async defer></script>
<script is:inline>
  function getTheme() {
    return document.documentElement.classList.contains("dark") ? "dark" : "light";
  }

  function renderHCaptcha(theme) {
    const container = document.getElementById("hcaptcha-container");
    if (!container || typeof hcaptcha === "undefined") return;

    container.innerHTML = ""; // clear previous instance if any

    hcaptcha.render("hcaptcha-container", {
      sitekey: "50b2fe65-b00b-4b9e-ad62-3ba471098be2",
      theme: theme,
      language: "cy",
    });
  }

  function attachThemeToggleListeners() {
    const switches = document.querySelectorAll("[data-theme-switcher]");

    if (!window._themeSwitcherListeners) {
      window._themeSwitcherListeners = new WeakSet();
    }

    switches.forEach((sw) => {
      if (!window._themeSwitcherListeners.has(sw)) {
        sw.addEventListener("click", () => {
          setTimeout(() => renderHCaptcha(getTheme()), 150);
        });
        window._themeSwitcherListeners.add(sw);
      }
    });
  }

  function setupCaptchaAndListeners() {
    renderHCaptcha(getTheme());
    attachThemeToggleListeners();
  }

  function waitForHCaptcha() {
    if (typeof hcaptcha !== "undefined") {
      setupCaptchaAndListeners();
    } else {
      setTimeout(waitForHCaptcha, 100);
    }
  }

  document.addEventListener("DOMContentLoaded", waitForHCaptcha);
  document.addEventListener("astro:after-swap", setupCaptchaAndListeners);
</script>